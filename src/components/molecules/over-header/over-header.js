import React from 'react'

import * as  topMarketingStyles from './over-header.module.scss'

const OverHeader = () => (
  <div className={`${topMarketingStyles.content}`} >
    <div className={`${topMarketingStyles.image}`}>
    </div>
  </div >
)
export default OverHeader